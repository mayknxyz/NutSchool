---
export interface CardContent {
  headingType?: 'h1' | 'h2' | 'h3';
  headingText?: string;
  paragraphText?: string;
}

export interface Props {
  cards?: CardContent[];
}

const { cards = [
  { headingType: 'h2', headingText: 'First Card', paragraphText: 'This is some placeholder content.' },
  { headingType: 'h2', headingText: 'Second Card', paragraphText: 'This is some placeholder content.' },
] } = Astro.props;
---
<section class="my-8">
    <div class="w-full max-w-full mx-auto">
        <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
            {cards.map(card => {
                const Heading = card.headingType || 'h2';
                return (
                    <div class="glass-card w-full px-8 py-20 relative text-center">
                        {card.headingText && <Heading>{card.headingText}</Heading>}
                        {card.paragraphText && <p>{card.paragraphText}</p>}
                    </div>
                )
            })}
        </div>
    </div>
</section>
